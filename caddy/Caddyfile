# Import subdomain configurations at top level
import /mnt/data/apps/*/caddy-subdomain.conf

doughughes.net, www.doughughes.net, dougco.dev, www.dougco.dev {
	# Import all app configurations
	# This includes the doughughes-net app which provides the catch-all handler
	import /mnt/data/apps/*/caddy.conf
}
